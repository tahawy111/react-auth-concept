import { Application } from "express";

export default function (app: Application) {
    // app.use("/api", auth);
    // app.use("/api", user);
    // app.use("/api", category);
    // app.use("/api", blog);
    // app.use("/api", comment);
  }